/**
 * Build field the type is `this` symbol.
 */
export const THIS_TYPE: Symbol = Symbol(
  '@bytegem/nest-gql-mix::this-type',
);

/**
 * Build field the type is list `this` symbol.
 */
export const LIST_THIS_TYPE = [THIS_TYPE];
